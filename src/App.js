import { hot }  from 'react-hot-loader';
import React from 'react';
import './App.css';

const App = () => (
        <div>
            <div className="App">
                <h1>Hello, World!</h1>
            </div>
        </div>
);

export default hot(module)(App);
// export default App;
